// Copyright 2024 Lie Yan

/**
 Links to a URL or a location in the document.
 */
class LinkNode: Node {
    // MARK: - Defining state

    /*
     A text defining the link target, either a url or a reprensentation of
     location in the document.
     */

    // MARK: - Query

    /*
     If a url, the url.
     If not, internal representation and displayed text
     of the location in the document.
     */
}
